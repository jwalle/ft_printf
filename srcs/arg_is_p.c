/* == HEADER == */

#include "ft_printf.h"

void	arg_is_p(t_arg *arg, va_list list)
{

	//if (arg->length->no)
	(void)arg;
		ft_atoi_hex(va_arg(list, void*));
	/*	va_arg(list, void*);
	else if (arg->length->j)
		va_arg(list, int);*/
}